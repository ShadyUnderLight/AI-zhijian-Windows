<Window x:Class="AIZhijian.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AIZhijian.Views"
        xmlns:vm="clr-namespace:AIZhijian.ViewModels"
        Title="AI 智剪"
        Width="1100" Height="750" MinWidth="960" MinHeight="680"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <!-- Login State View -->
        <ContentControl x:Name="LoginStateView" Visibility="Visible">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="正在验证登录状态..." FontSize="18" Foreground="{StaticResource TextSecondary}"
                           HorizontalAlignment="Center" Margin="0,0,0,12"/>
            </StackPanel>
        </ContentControl>

        <!-- Login Page -->
        <local:LoginPage x:Name="LoginPageControl" Visibility="Collapsed"/>

        <!-- Main Layout -->
        <Grid x:Name="MainLayout" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                    Padding="16,10">
                <Grid>
                    <TextBlock FontSize="15" FontWeight="SemiBold">
                        <Run Text="AI 智剪  "/>
                        <Run x:Name="UserInfoRun" Text="" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    </TextBlock>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="SettingsButton" Content="⚙ 设置" Style="{StaticResource SecondaryButton}"
                                Click="Settings_Click" Margin="0,0,8,0"/>
                        <Button x:Name="LogoutButton" Content="退出" Style="{StaticResource SecondaryButton}"
                                Click="Logout_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Body: Sidebar + Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource SidebarBg}" BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,1,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,8,0,0">
                            <Button Content="📊 仪表盘"    Tag="Dashboard"   Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Button Content="🖼️ 图片生成"  Tag="ImageGen"    Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Button Content="🍌 Banana"     Tag="Banana"      Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Button Content="🎬 Seedance"   Tag="Seedance"    Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Button Content="🎥 Wan"        Tag="Wan"         Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Button Content="🌐 Veo"        Tag="Veo"         Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Button Content="🤖 Grok"       Tag="Grok"        Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Separator Margin="12,6" Background="{StaticResource BorderBrush}"/>
                            <Button Content="⏳ 任务队列"   Tag="TaskList"    Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                            <Button Content="🎨 作品画廊"   Tag="WorksGallery" Style="{StaticResource SidebarButton}" Click="Nav_Click"/>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Content Area -->
                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                    <ContentControl x:Name="ContentArea" Margin="16"/>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
